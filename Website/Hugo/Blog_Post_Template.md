---
title: "{{ TITLE }}"
date: {{ DATE }}
hero: "/images/posts/{{ HERO_IMAGE }}"
description: "{{ SHORT_DESCRIPTION }}"
summary: "{{ SHORT_SUMMARY }}"
categories:
  - "{{ CATEGORY_1 }}"
  - "{{ CATEGORY_2 }}"
tags:
  - "{{ TAG_1 }}"
  - "{{ TAG_2 }}"
  - "{{ TAG_3 }}"
draft: true
toc: true
menu:
  sidebar:
    name: "{{ SIDEBAR_NAME }}"
    identifier: "{{ SIDEBAR_IDENTIFIER }}"
    parent: "{{ SIDEBAR_PARENT }}"
    weight: {{ SIDEBAR_WEIGHT }}
---

# Title

## Sub Title

---
